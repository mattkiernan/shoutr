<% if current_user != user %>
  <% if current_user.following?(user) %>
    <%= button_to "Unfollow", [user, :following_relationship], method: :delete,
class: "follow-button" %>
  <% else %>
    <%= button_to "Follow", [user, :following_relationship], class:
"follow-button" %>
  <% end %>
<% end %>
